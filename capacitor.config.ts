import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'at.zcontrol.zoe.image_to_text',
  appName: 'z-control Image to Text',
  webDir: 'www'
};

export default config;
